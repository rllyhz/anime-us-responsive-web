<%- include("../includes/header.ejs") %>

  <div class="row">
    <div class="col-sm-2 col-md-2 col-lg-2 col-xl-2">
      <form action="" id="search-manga">
        <div class="form-group">
          <input id="search" name="search" type="text" placeholder="Search your fav Manga ..." class="form-control" />
          <label for="search" hidden>Search</label>
        </div>
      </form>
    </div>
  </div>

  <div class="row mt-6">
    <div class="col-sm-1 col-md-2 col-lg-3 co-xl-4">
      <% mangaList.forEach(manga=> { %>
        <div class="anime" style="margin: .56rem;">
          <div class="card" style="padding-top: 2%;">
            <div class="card-image">
              <a href="/manga/show/<%= manga.mal_id %>">
                <img src="<%= manga.image_url %>" alt="<%= manga.title %>">
              </a>
            </div>
            <div class="card-title">
              <a href="/manga/show/<%= manga.mal_id %>" style="color: white; text-decoration: underline;">
                <%= manga.title %>
              </a>
            </div>
          </div>
        </div>
        <% }) %>
    </div>
  </div>

  <%- include("../includes/footer.ejs") %>